function[A] = makeAdj(n, states)
Map = expansion(n, states);
A=zeros(states, states);

for i=1:n
    for j=1:n
       if (j~=n)
           if (Map(i, j) ~= Map(i, j+1))
               A(
           end
               
                
      
       end 
    end        
end